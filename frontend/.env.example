# Environment Variables
# OAuth server URL for OpenShift authentication
VITE_OAUTH_URL=https://oauth-openshift.apps.example.com/oauth/authorize

# Development-only: bypass OAuth with a static token
VITE_DEV_TOKEN=

# API base URL (defaults to same origin)
VITE_API_BASE_URL=
