apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-solr
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}-solr
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
    - port: 8983
      targetPort: 8983
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}-solr
    app.kubernetes.io/instance: {{ .Release.Name }}
