apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-okp-mcp
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}-okp-mcp
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.okpMcp.port }}
      targetPort: {{ .Values.okpMcp.port }}
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}-okp-mcp
    app.kubernetes.io/instance: {{ .Release.Name }}
